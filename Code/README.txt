* kmeans.py is a python script which can be executed from a terminal and takes command line arguments

*   This code accepts two arguments:
           1. file path (where your input data is kept)
           2. Number of Clusters

           Press enter to execute.